{
  "name": "ops-host",
  "version": "0.3.2",
  "private": true,
  "type": "module",
  "description": "OpenPrintShare Host daemon — Core + Protocol + Virtual Printer (MockPrinterBackend)",
  "scripts": {
    "dev": "bun --watch index.ts",
    "start": "bun index.ts"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1",
    "socket.io": "^4.8.1"
  }
}
